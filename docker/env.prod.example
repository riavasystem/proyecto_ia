# Copiar a /opt/apps/proyecto_ia/.env en el servidor. NUNCA subir el .env real al repo.

BACKEND_IMAGE=ghcr.io/riavasystem/proyecto_ia-backend:latest

POSTGRES_USER=proyecto_ia
POSTGRES_PASSWORD=changeme
POSTGRES_DB=proyecto_ia

ENVIRONMENT=production
DEBUG=false

JWT_SECRET=changeme
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=30

CORS_ORIGINS=["https://admin.tuplataforma.com"]
